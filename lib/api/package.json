{
  "name": "ive-hub-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install": "npm install --prefix layer/nodejs",
    "test": "docker run --rm -v $(pwd)/src:/var/task:ro,delegated -v $(pwd)/layer:/opt:ro,delegated lambci/lambda:nodejs12.x index.handler"
  },
  "author": "Miklet",
  "dependencies": {
    "@aws-crypto/client-node": "^1.0.2",
    "aws-sdk": "^2.592.0",
    "chrome-aws-lambda": "^2.0.1",
    "moment": "^2.24.0",
    "moment-timezone": "^0.5.35",
    "puppeteer-core": "^2.0.0",
    "slugify": "^1.3.6"
  }
}
